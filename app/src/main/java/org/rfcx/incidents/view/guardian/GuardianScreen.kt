package org.rfcx.incidents.view.guardian

enum class GuardianScreen(val value: Int) {
    CONNECT(-2), CHECKLIST(-1), SOFTWARE_UPDATE(0), CLASSIFIER_UPLOAD(1), POWER_DIAGNOSTIC(2), NETWORK_TEST(5)
}
